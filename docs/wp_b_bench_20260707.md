# WP-B Phase 8 Hardware Gate - 2026-07-07

Phase 8 was run with two T3S3 nodes attached to this host. The gate initially
exposed max-frame KISS sizing, ARQ/MAC pacing, host-ingress buffering, and USB
egress issues. Those were fixed during the gate. Final result: both directions
passed the 30 x 1280-byte zero-gap hardware burst with no corruption,
duplicates, or missing frames.

## Firmware Under Test

- Git HEAD: `0936bc2`
- Working tree: dirty; includes uncommitted WP-B Phase 6 MAC integration,
  Phase 7 bench-tooling changes, and Phase 8 hardware-gate fixes.

Verification after the decoder fix:

```sh
cd firmware
pio test -e native
pio run -e t3s3
```

Final verification:

- `pio test -e native`: 73/73 passed.
- `pio run -e t3s3-serial-wdt`: passed.

## Connected Hardware

`pio device list` reported two usable USB CDC boards:

```text
/dev/ttyACM0
USB VID:PID=303A:1001 SER=9C:13:9E:F1:0A:34 LOCATION=2-2:1.0

/dev/ttyACM1
USB VID:PID=303A:1001 SER=9C:13:9E:F1:0A:2C LOCATION=2-4:1.0
```

Both nodes were flashed with the same image:

```sh
cd firmware
pio run -e t3s3 -t upload --upload-port /dev/ttyACM0
pio run -e t3s3 -t upload --upload-port /dev/ttyACM1
```

Both uploads passed with esptool flash verification.

## Baseline STATS

After flashing the corrected image and allowing the link to settle, both nodes
reported `linkState=READY` with zero data-path error counters:

```text
/dev/ttyACM0: linkReady=1 linkState=READY hbTx=2 hbAckRx=2 v3VerDrop=0 v3TypeDrop=0 arqV3Retry=0 arqV3TxDone=0
/dev/ttyACM1: linkReady=1 linkState=READY hbTx=3 hbAckRx=3 v3VerDrop=0 v3TypeDrop=0 arqV3Retry=0 arqV3TxDone=0
```

## Decoder Capacity Fix

The first 30 x 1280-byte run delivered no RF data. Root cause: the firmware
KISS decoder still capped decoded KISS payloads at `TNC_PAYLOAD_MAX_LEN`
1280 bytes, but generic-mode USB input carries an 8-byte serial-integrity
header, so a max payload arrives as a 1288-byte KISS payload.

Fix applied during Phase 8:

- `KissFrame` and the internal KISS decode buffer now use
  `KISS_FRAME_PAYLOAD_MAX_LEN = TNC_PAYLOAD_MAX_LEN + SERIAL_INTEGRITY_HDR_LEN`.
- The legacy `Kiss::decode(PayloadFrame&)` helper rejects decoded frames above
  `TNC_PAYLOAD_MAX_LEN` before copying into `PayloadFrame`.
- Native KISS tests now include max-size wrapped generic frame coverage.

## Fixes Applied During Gate

- Raised firmware KISS decode capacity for generic-mode USB frames to
  `TNC_PAYLOAD_MAX_LEN + SERIAL_INTEGRITY_HDR_LEN` while keeping
  `PayloadFrame` capped at 1280 bytes.
- Added a v3 DATA round-end flag and deferred partial ARQ ACKs until the end of
  a transmit round. This avoids fragment-by-fragment ACK/DATA contention on the
  half-duplex radio path.
- Used the existing `RADIO_INTER_FRAG_DELAY_MS` pacing for v3 DATA sends so the
  peer has time to re-arm RX between packets.
- Raised `TX_QUEUE_DEPTH` from 8 to 32 so firmware can absorb the Phase 8
  zero-gap 30-frame USB ingress burst while the radio drains at paced speed.
- Chunked USB CDC serial TX writes to 64-byte pieces with a one-tick yield
  between chunks, which removed the observed one-frame host serial-integrity
  truncation in the reverse-direction run.

## Zero-Gap Burst Results

Command shape:

```sh
# Receiver
python3 pi-daemon/raw_fragment_test.py --port /dev/ttyACM0 \
  --boot-wait 5 listen --idle-timeout 300 --expected 30 --stats --stats-timeout 5

# Sender
python3 pi-daemon/raw_fragment_test.py --port /dev/ttyACM1 \
  --boot-wait 5 send --stress-1280 --hold-after-send 180 --stats --stats-timeout 10
```

Forward result (`/dev/ttyACM1` -> `/dev/ttyACM0`):

```text
RX summary
  frames=30 valid_unique=30 bad=0 duplicates=0 duplicate_conflicts=0 missing_in_range=0
  seq_range=1..30
  len=1280 frags=12 count=30
```

Receiver STATS:

```text
rx=30 rxBytes=38400 arqDone=30 arqMetaDrop=0 arqIntDrop=0 arqCrc=0
qTx=0/32 qRx=0/8 arqTx=0/0 arqRx=0 arqAck=0 arqCred=4 arqPool=8/8
```

Sender STATS:

```text
TX STATS OK ... arqV3Retry=0 arqV3Sat=0 arqV3Bad=0 arqV3Credit=0
arqV3Alloc=0 arqV3TxDone=30 ... qTx=0/32 ... arqTx=0/0
```

Reverse result (`/dev/ttyACM0` -> `/dev/ttyACM1`):

```text
RX summary
  frames=30 valid_unique=30 bad=0 duplicates=0 duplicate_conflicts=0 missing_in_range=0
  seq_range=1..30
  len=1280 frags=12 count=30
```

Reverse receiver STATS:

```text
rx=30 rxBytes=38400 arqDone=30 arqMetaDrop=0 arqIntDrop=0 arqCrc=0
qTx=0/32 qRx=0/8 arqTx=0/0 arqRx=0 arqAck=0 arqCred=4 arqPool=8/8
```

Reverse sender STATS:

```text
TX STATS OK ... arqV3Retry=0 arqV3Sat=0 arqV3Bad=0 arqV3Credit=0
arqV3Alloc=0 arqV3TxDone=30 ... qTx=0/32 ... arqTx=0/0
```

## Gate Status

Phase 8 passes the two-board zero-gap 1280-byte hardware gate.

- Both boards build and flash with the same image.
- Both boards reach `linkState=READY` after boot.
- 30 x 1280-byte zero-gap burst passes forward and reverse.
- Delivered payloads are byte-correct end to end.
- Duplicate delivery was not observed.
- Serial-integrity drops were not observed in the final runs.
- Sender ARQ completed 30/30 datagrams with no retry exhaustion, saturation,
  malformed input, credit withdrawal, or allocation failure counters.

## Induced-Loss Run

The low-power profile was restored on both boards and the abusive 30 x 1280-byte
burst was rerun from `/dev/ttyACM1` to `/dev/ttyACM0`:

- `power=-18` on both nodes.
- `raw_fragment_test.py send --stress-1280 --hold-after-send 60 --stats`.
- Receiver summary: `frames=30 valid_unique=30 bad=0 duplicates=0
  duplicate_conflicts=0 missing_in_range=0`.
- Sender summary: `arqV3TxDone=30`, `arqV3Retry=0`, `arqV3Sat=0`,
  `arqV3Credit=0`.

This run remained clean under the induced-loss profile. It did not surface
corruption, duplication, or retry exhaustion.

## Egress Blockage And Bidirectional Pressure

I exercised the host-side blockage path with a no-read receiver on
`/dev/ttyACM0` and a larger zero-gap burst from `/dev/ttyACM1`:

- `raw_fragment_test.py send --sizes 1280 --count 80 --interval-ms 0`.
- Sender completed 80 writes and reported `arqV3TxDone=79` at shutdown.
- Reopening `/dev/ttyACM0` after the blocked period did not recover any queued
  payloads; the reopen returned `frames=0`.

I also ran concurrent transmit pressure by starting the same 80-frame zero-gap
burst on both `/dev/ttyACM0` and `/dev/ttyACM1` at the same time. Both helpers
completed the transmit loop, but the final `STATS` queries timed out, so that
run is not yet a clean closeout artifact.

## Next Debug Target

The next phase still needs a better receiver-side harness before Phase 9 can be
closed cleanly:

- Capture a receiver-side blocked-egress `STATS` snapshot during the hold.
- Re-run concurrent bidirectional pressure with a listener harness that can
  preserve and replay queued host output.
- Consider reducing diagnostic-only serial TX pacing after broader validation if
  throughput needs tuning.

## 2026-07-08 Zero-Credit Stall Fix Retest

Firmware commit `2ba7b54` (`Fix ARQ zero-credit stall with delayed probe`) was
flashed to both attached boards:

```text
/dev/ttyACM0  USB VID:PID=303A:1001 SER=9C:13:9E:F1:0A:34
/dev/ttyACM1  USB VID:PID=303A:1001 SER=9C:13:9E:F1:0A:2C
```

Verification before flashing:

```text
pio test -e native  -> 75/75 passed
pio run -e t3s3     -> passed
```

Both uploads passed esptool flash verification. The same 30 x 1280-byte
zero-gap hardware gate was then rerun in both directions.

Forward result (`/dev/ttyACM1` -> `/dev/ttyACM0`):

```text
RX summary
  frames=30 valid_unique=30 bad=0 duplicates=0 duplicate_conflicts=0 missing_in_range=0
  seq_range=1..30
  len=1280 frags=12 count=30

RX STATS: rx=30 rxBytes=38400 arqDone=30 arqCrc=0 arqV3Retry=0
          arqV3Credit=0 arqV3TxDone=0 arqV3Probe=0 linkState=READY
TX STATS: arqV3TxDone=30 arqV3Retry=0 arqV3Sat=0 arqV3Bad=0
          arqV3Credit=0 arqV3Alloc=0 arqV3Probe=0 linkState=READY
```

Reverse result (`/dev/ttyACM0` -> `/dev/ttyACM1`):

```text
RX summary
  frames=30 valid_unique=30 bad=0 duplicates=0 duplicate_conflicts=0 missing_in_range=0
  seq_range=1..30
  len=1280 frags=12 count=30

RX STATS: rx=30 rxBytes=38400 arqDone=30 arqCrc=0 arqV3Retry=0
          arqV3Credit=0 arqV3TxDone=0 arqV3Probe=0 linkState=READY
TX STATS: arqV3TxDone=30 arqV3Retry=0 arqV3Sat=0 arqV3Bad=0
          arqV3Credit=0 arqV3Alloc=0 arqV3Probe=0 linkState=READY
```

The reverse sender reported one `v3VerDrop` while still completing all 30
datagrams with no ARQ retry, credit-withdrawal, allocation-failure, or probe
counter increments. Reopening both boards for fresh idle `STATS` reset the
devices and both came back `READY` with clean data-path counters.

The zero-credit probe did not need to fire in this healthy drain test
(`arqV3Probe=0` in both directions), which is the expected steady-state result.
The next hardware check should deliberately recreate host egress blockage and
confirm that `arqV3Probe` increments once, the sender unwedges, and delivery
continues after the receiver drains.

## 2026-07-08 Blocked-Egress Probe Attempts

After the healthy two-board gate, I tried to recreate the old egress-blocked
credit-starvation condition by opening `/dev/ttyACM0` and intentionally not
reading from it while sending max-size frames from `/dev/ttyACM1`.

Attempt 1:

```text
Receiver: /dev/ttyACM0 held open without reads for 180 s
Sender:   /dev/ttyACM1 sent 80 x 1280-byte frames, then held for 180 s
Result:   TX STATS returned arqV3TxDone=80, arqV3Retry=0,
          arqV3Credit=0, arqV3Alloc=0, arqV3Probe=0
```

This did not recreate the blocked-egress condition. The host/USB path absorbed
the receiver's output well enough that the sender completed all 80 datagrams
without credit withdrawal or probing.

Attempt 2:

```text
Receiver: /dev/ttyACM0 held open without reads for 360 s
Sender:   /dev/ttyACM1 sent 300 x 1280-byte frames, then held for 300 s
Result:   sender TX STATS timed out
```

This run stressed the control path, but the timing was malformed for recovery
validation: the sender queried `STATS` before the receiver hold had released.
It is not evidence for or against the zero-credit probe.

Attempt 3:

```text
Receiver: /dev/ttyACM0 held open without reads for 180 s
Sender:   /dev/ttyACM1 sent 300 x 1280-byte frames, then held for 360 s
Result:   TX STATS returned, but all ARQ counters were zero:
          arqV3TxDone=0, arqV3Retry=0, arqV3Credit=0, arqV3Probe=0
```

This is also inconclusive. The sender was responsive after the recovery window,
but the zeroed ARQ counters mean the run did not preserve the same useful
counter epoch, likely due to reset or an invalid stress setup. It cannot be
used as a probe-path pass/fail result.

Conclusion: the current no-read-host method is not a reliable blocked-egress
proof harness. The next validation step should add a deterministic receiver
egress-block mode or diagnostic command in firmware, so the receiver can report
zero credits while keeping the sender-side control path and counters observable.

## 2026-07-08 Deterministic Egress Diagnostic

Added a firmware KISS control diagnostic:

```text
DIAG                         -> report current diagnostic egress mode
DIAG egress=open             -> normal receiver egress capacity
DIAG egress=blocked          -> force receiver credits to zero
DIAG egress=oneshot          -> allow one delivery, then report zero credits
```

`STATS` now includes `diagEgress`:

```text
diagEgress=0  open
diagEgress=1  blocked
diagEgress=2  one-shot zero-credit
```

The one-shot mode was added because the zero-credit probe regression is not the
same as a hard egress block. The probe path requires an ACK that completes the
currently open datagram while advertising `receiver_credits=0`, leaving queued
work and no active datagram in flight. A hard block exercises the
credit-withdrawal path instead.

Verification:

```text
pio test -e native  -> 75/75 passed
pio run -e t3s3     -> passed
```

Both boards were flashed with the diagnostic build.

Probe reproduction:

```text
Receiver: /dev/ttyACM0 DIAG egress=oneshot
Sender:   /dev/ttyACM1 sent 4 x 1280-byte frames, held 15 s, queried STATS
Result:   arqV3TxDone=1, arqV3Retry=3, arqV3Credit=0, arqV3Probe=3
```

This confirms that the hardware path can now force the delayed zero-credit
probe branch and that the sender stays locally observable while probes fire.

Recovery check:

```text
Receiver: /dev/ttyACM0 DIAG egress=oneshot
Sender:   /dev/ttyACM1 sent 2 x 1280-byte frames, held 30 s
Action:   receiver was cleared with DIAG egress=open during the sender hold
Result:   sender STATS returned arqV3Probe=1, arqV3Retry=2, arqV3TxDone=0
```

The probe branch is proven on hardware, but full post-clear delivery recovery
is not yet closed. The remaining issue is that clearing the receiver diagnostic
did not produce `arqV3TxDone` completion in the captured sender counter epoch.
Next work should focus on the receiver's release path after a one-shot
zero-credit ACK, rather than on the original no-deadline sender wedge.

## 2026-07-08 Deterministic Egress Recovery Closure

Root cause of the failed recovery check was twofold:

- Generic-mode `serviceTx()` returned while waiting for a heartbeat ACK, so ARQ
  receive-completion work could be skipped during heartbeat wait windows.
- v3 `CREDIT_WITHDRAWAL` ACKs set an immediate retry deadline. On hardware,
  the sender could repeatedly retransmit and exhaust the active slot before the
  receiver egress was reopened.

Fixes:

- Continue ticking the v3 ARQ engine while generic heartbeat wait is active.
- Pace `CREDIT_WITHDRAWAL` retransmission with the credit-stall interval and
  reset the slot attempt budget while the receiver is actively replying with
  backpressure.
- Add native regression coverage for repeated withdrawal ACKs beyond the old
  max-attempt exhaustion point.
- Promote ACK TX/RX/error counters and compact v3 ARQ state into normal
  `STATS` output for hardware diagnostics.

Verification:

```text
git diff --check  -> passed
pio test -e native -> 76/76 passed
pio run -e t3s3    -> passed
```

Both boards were flashed with the recovery build.

Passing held-open recovery run:

```text
Receiver controller: /dev/ttyACM0 held open, DIAG egress=oneshot
Sender:              /dev/ttyACM1 sent 2 x 1280-byte frames, held 40 s
Action:              receiver reopened live with DIAG egress=open

Receiver STATS:
  rx=2 rxBytes=2560 arqDone=2 ackTx=65 ackTxErr=2
  arqV3Credit=18 arqV3Retry=0 diagEgress=0

Sender STATS:
  ackRx=19 arqV3Retry=0 arqV3TxDone=2 arqV3Probe=0
  arqTx=0/0 arqCred=3
```

The deterministic blocked-then-open receiver path now recovers: both datagrams
are delivered to receiver egress and both sender TX slots complete without retry
exhaustion.
